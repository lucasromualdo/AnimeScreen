<Window x:Class="Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MyAnimeScreen"
        Height="520"
        Width="920"
        MinHeight="400"
        MinWidth="700">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock FontSize="28"
                   FontWeight="SemiBold"
                   Text="MyAnimeScreen" />

        <TextBlock Grid.Row="1"
                   Margin="0,12,0,20"
                   Foreground="#555555"
                   Text="Estrutura base criada: dados locais com SQLite + repositórios prontos para integração com API." />

        <Border Grid.Row="2"
                BorderBrush="#DDDDDD"
                BorderThickness="1"
                CornerRadius="8"
                Padding="16">
            <StackPanel>
                <TextBlock FontSize="16"
                           FontWeight="SemiBold"
                           Text="Próximos passos (MVP)" />
                <TextBlock Margin="0,8,0,0" Text="1. Busca por nome com Jikan API" />
                <TextBlock Margin="0,6,0,0" Text="2. Tela de detalhes do anime" />
                <TextBlock Margin="0,6,0,0" Text="3. Minha Lista (status + episódio atual)" />
                <TextBlock Margin="0,6,0,0" Text="4. Filtros por status/gênero/nota" />
            </StackPanel>
        </Border>
    </Grid>
</Window>
