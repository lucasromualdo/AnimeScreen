<Window x:Class="Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MyAnimeScreen"
        Height="620"
        Width="920"
        MinHeight="400"
        MinWidth="700">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter" />
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock FontSize="28"
                   FontWeight="SemiBold"
                   Text="MyAnimeScreen" />

        <TextBlock Grid.Row="1"
                   Margin="0,10,0,18"
                   Foreground="#555555"
                   Text="Busque animes por nome para carregar resultados da Jikan API." />

        <Grid Grid.Row="2" Margin="0,0,0,14">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBox Grid.Column="0"
                     Margin="0,0,10,0"
                     Padding="10,6"
                     FontSize="14"
                     Text="{Binding Query, UpdateSourceTrigger=PropertyChanged}" />

            <Button Grid.Column="1"
                    MinWidth="110"
                    Padding="14,8"
                    Command="{Binding SearchCommand}"
                    Content="Buscar" />
        </Grid>

        <StackPanel Grid.Row="3">
            <TextBlock Foreground="#B91C1C"
                       FontWeight="SemiBold"
                       Margin="0,0,0,10"
                       Text="{Binding ErrorMessage}" />

            <TextBlock Margin="0,0,0,10"
                       FontWeight="SemiBold"
                       Foreground="#1F2937"
                       Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}"
                       Text="Carregando resultados..." />

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*" />
                    <ColumnDefinition Width="16" />
                    <ColumnDefinition Width="3*" />
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0"
                        BorderBrush="#D1D5DB"
                        BorderThickness="1"
                        CornerRadius="8"
                        Padding="10">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <TextBlock FontSize="15"
                                   FontWeight="SemiBold"
                                   Text="Resultados" />

                        <ListBox Grid.Row="1"
                                 Margin="0,8,0,0"
                                 ItemsSource="{Binding Results}"
                                 SelectedItem="{Binding SelectedAnime, Mode=TwoWay}">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Border Padding="4">
                                        <StackPanel>
                                            <TextBlock FontWeight="SemiBold"
                                                       Text="{Binding Title}" />
                                            <TextBlock Margin="0,2,0,0"
                                                       FontSize="12"
                                                       Foreground="#6B7280"
                                                       Text="{Binding Score, StringFormat=Nota: {0:F2}}" />
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </Grid>
                </Border>

                <Border Grid.Column="2"
                        BorderBrush="#D1D5DB"
                        BorderThickness="1"
                        CornerRadius="8"
                        Padding="12">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <StackPanel>
                            <TextBlock FontSize="18"
                                       FontWeight="SemiBold"
                                       Text="{Binding SelectedAnime.Title}" />

                            <TextBlock Margin="0,4,0,0"
                                       Foreground="#6B7280"
                                       Text="{Binding SelectedAnime.TitleJp, StringFormat=Título JP: {0}}" />

                            <TextBlock Margin="0,10,0,0"
                                       Text="{Binding SelectedAnime.Score, StringFormat=Nota: {0:F2}}" />

                            <TextBlock Margin="0,4,0,0"
                                       Text="{Binding SelectedAnime.EpisodesTotal, StringFormat=Episódios: {0}}" />

                            <TextBlock Margin="0,4,0,0"
                                       Text="{Binding SelectedAnime.Year, StringFormat=Ano: {0}}" />

                            <TextBlock Margin="0,4,0,0"
                                       Text="{Binding SelectedAnime.Season, StringFormat=Temporada: {0}}" />

                            <TextBlock Margin="0,14,0,0"
                                       FontWeight="SemiBold"
                                       Text="Sinopse" />

                            <TextBlock Margin="0,6,0,0"
                                       TextWrapping="Wrap"
                                       Text="{Binding SelectedAnime.Synopsis}" />
                        </StackPanel>
                    </ScrollViewer>
                </Border>
            </Grid>
        </StackPanel>
    </Grid>
</Window>
